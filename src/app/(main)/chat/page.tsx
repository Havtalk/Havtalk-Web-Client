"use client";

import React, { useEffect, useRef, useState } from "react";
import { Button } from "@/components/ui/button";
import { ArrowRight, MessageSquare, Sparkles, Star } from "lucide-react";
import Image from "next/image";

export default function Chat() {
  
  return (
    <section
      className="relative overflow-hidden pt-16 pb-16 md:pt-20 md:pb-20 h-auto min-h-[calc(100vh-4rem)] md:min-h-[calc(100vh-4rem)] flex items-center justify-center bg-gradient-to-b from-black to-gray-900 "
    >
      chat
    </section>
  );
}